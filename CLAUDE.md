# Project instructions

- On every coding task in this repo (writing, adding, refactoring, fixing, reviewing, or designing code), apply the `ponytail` skill: prefer the simplest, shortest, most minimal solution — stdlib/native features before new dependencies, no speculative abstractions.
- Keep responses compressed using the `caveman` skill style (terse, high-signal, no filler) by default in this project, unless the user asks for full detail.
